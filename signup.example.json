{
	"username": [
		{ "required": "username required" },
		{ "minimumLength:3": "3 minimum" },
		{ "maximumLength:10": "10 maximum" },
		{ "regex:/^[a-zA-z0-9]*$/": "alphanumeric only" }
	],

	"number": [
		{ "type:number": "must be of type number" }
	],
	"string": [
		{ "type:string": "must be of type string" }
	],
	"boolean": [
		{ "type:boolean": "must be of type boolean" }
	],
	"object": [
		{ "type:object": "must be of type object" }
	],
	"null": [
		{ "type:null": "must be of type null" }
	],

	"lessThan": [
		{ "<:0": "must be less than zero" }
	],
	"lessThanOrEqualTo": [
		{ "<=:0": "must be less than or equal to zero" }
	],
	"greaterThan": [
		{ ">:0": "must be greater than zero" }
	],
	"greaterThanOrEqualTo": [
		{ ">=:0": "must be greater than or equal to zero" }
	],
	"equalTo": [
		{ "==:0": "must be equal to zero" }
	],

	"email": [
		{ "email": "bad email format" }
	],

	"match": [
		{ "match": "values must match" }
	]
}
